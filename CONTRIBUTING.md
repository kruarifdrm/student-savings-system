# ร่วมพัฒนา / แจ้งบั๊ก

ขอบคุณที่ช่วยกันทำให้ระบบนี้ดีขึ้นครับ 🙏
ไม่ต้องเป็นโปรแกรมเมอร์ก็ช่วยได้ — แค่แจ้งว่าเจอปัญหาอะไรก็มีประโยชน์มากแล้ว

---

## แบบที่ 1 · แค่อยากแจ้งบั๊ก (ไม่ต้องแตะโค้ด)

1. เข้าหน้า repo → แท็บ **Issues** → ปุ่มเขียว **New issue**
2. เลือกแบบฟอร์ม **แจ้งบั๊ก** แล้วกรอกตามหัวข้อ
3. กด **Submit new issue**

บอกมาแค่นี้ก็พอ: ทำอะไรอยู่ → คาดว่าจะเกิดอะไร → จริง ๆ เกิดอะไร
ถ้าแนบรูปหน้าจอมาด้วยจะช่วยได้มาก (ลากรูปมาวางในช่องพิมพ์ได้เลย)

> ⚠️ **อย่าแนบชื่อ-สกุลนักเรียนจริงหรือยอดเงินจริง** ในรูปหรือข้อความ
> ปิดชื่อก่อน หรือใช้ชื่อสมมติแทน

---

## แบบที่ 2 · แก้โค้ดเองแล้วส่งกลับมา (Pull Request)

คนนอกจะ push เข้า repo นี้ตรง ๆ ไม่ได้ ต้อง **fork** ไปแก้ในสำเนาของตัวเองก่อน
แล้วส่ง Pull Request กลับมา เจ้าของ repo จะเป็นคนกดรับ

### วิธีที่ง่ายที่สุด — แก้บนเว็บ GitHub เลย (เหมาะกับแก้ไม่กี่บรรทัด)

1. เปิดไฟล์ที่จะแก้บน GitHub → กดไอคอน **ดินสอ ✏️** มุมขวาบน
2. GitHub จะขึ้นว่า *"You need to fork this repository to propose changes"* → กด **Fork this repository**
3. แก้ข้อความในโค้ด → เลื่อนลงล่าง → กด **Commit changes...**
   - ช่องบน: สรุปสั้น ๆ ว่าแก้อะไร เช่น `แก้ยอดรวมคำนวณผิดเมื่อลบรายการ`
   - เลือก **Create a new branch and start a pull request**
4. กด **Propose changes** → **Create pull request**

เท่านี้เสร็จครับ ไม่ต้องลง git ไม่ต้องใช้ Terminal เลย

### วิธีเต็ม — แก้ในเครื่อง (เหมาะกับแก้เยอะ หรืออยากทดสอบก่อน)

```bash
# 1. กด Fork บนหน้า repo ก่อน แล้ว clone ของตัวเองลงมา
git clone https://github.com/<ชื่อบัญชีของคุณ>/student-savings-system.git
cd student-savings-system

# 2. แตกกิ่งใหม่ ตั้งชื่อให้รู้ว่าทำอะไร
git checkout -b fix/ยอดรวมคำนวณผิด

# 3. แก้ไฟล์ แล้วทดสอบ (ดูหัวข้อ "ทดสอบก่อนส่ง" ด้านล่าง)

# 4. commit และ push ขึ้น fork ของตัวเอง
git add -A
git commit -m "แก้ยอดรวมคำนวณผิดเมื่อลบรายการฝาก"
git push -u origin fix/ยอดรวมคำนวณผิด
```

จากนั้นเปิดหน้า fork ของตัวเองบน GitHub จะมีแถบเหลืองขึ้นมาว่า
**Compare & pull request** — กดแล้วเขียนอธิบายว่าแก้อะไร ทำไม แล้วกดส่ง

---

## ทดสอบก่อนส่ง

โปรเจกต์นี้เป็น HTML ล้วน ไม่มีขั้นตอน build และยังไม่มีชุดทดสอบอัตโนมัติ
ก่อนส่ง PR ขอให้เปิดใช้จริงกับโปรเจกต์ Firebase ของตัวเองแล้วลองตามนี้

- [ ] เปิดหน้าเว็บแล้ว **ไม่มี error สีแดงใน Console** ของเบราว์เซอร์ (กด F12)
- [ ] เข้าเป็น **ผู้ปกครอง** ได้ เห็นยอด และ**ไม่เห็นปุ่มฝาก-ถอน-ลบ**
- [ ] เข้าเป็น **ครู** ได้ ฝากและถอนได้ ยอดตรง
- [ ] ลบรายการแล้วยอดกลับมาถูกต้อง
- [ ] ทดสอบบนมือถือหรือย่อหน้าจอให้แคบ แล้วหน้าจอยังใช้งานได้

## แนวทางเขียนโค้ด

ยึดตามสไตล์ที่มีอยู่เดิม เพื่อให้ครูที่ไม่ใช่โปรแกรมเมอร์อ่านโค้ดตามได้

- **ภาษาไทยในคอมเมนต์** อธิบายว่า "ทำไม" ไม่ใช่ "ทำอะไร"
- **ไม่เพิ่ม dependency / build step / framework** — ต้องเปิดไฟล์แล้วใช้ได้เลย
- ใช้ `var` และ `function` แบบเดิม (ES5) ให้เข้ากับโค้ดส่วนที่เหลือ
- **ทุกครั้งที่เอาข้อความจากฐานข้อมูลมาแสดงด้วย `innerHTML` ต้องผ่าน `esc()` เสมอ**
- แก้ทีละเรื่อง — PR หนึ่งอันต่อหนึ่งปัญหา จะตรวจและรับง่ายกว่ามาก

## ถ้าแตะเรื่องสิทธิ์หรือความปลอดภัย

`firestore.rules` คือด่านกันจริงฝั่งเซิร์ฟเวอร์ การซ่อนปุ่มบนหน้าจอไม่ใช่การกัน
ถ้า PR แตะไฟล์นี้ หรือแตะการเช็คสิทธิ์ครู ช่วยเขียนอธิบายให้ละเอียดหน่อยว่า
หลังแก้แล้ว **ใครทำอะไรได้บ้าง** เพราะระบบนี้เก็บข้อมูลของเด็ก

**เจอช่องโหว่ความปลอดภัย อย่าเปิดเป็น Issue สาธารณะ** — ทักหาเจ้าของ repo โดยตรงก่อน
เพื่อให้แก้เสร็จก่อนที่คนอื่นจะเอาไปใช้

---

## สิ่งที่อยากได้ความช่วยเหลือเป็นพิเศษ

- ทดสอบบนมือถือรุ่น/เบราว์เซอร์ต่าง ๆ แล้วรายงานปัญหาหน้าจอ
- ปรับข้อความให้เข้าใจง่ายขึ้นสำหรับครูที่ไม่คุ้นเทคโนโลยี
- ทำคู่มือติดตั้งแบบมีภาพประกอบ
- ฟีเจอร์ที่ครูใช้จริงแล้วรู้สึกว่าขาด (บอกมาก่อนลงมือทำ จะได้ไม่เสียแรงเปล่า)

โค้ดที่ส่งมาถือว่าเผยแพร่ภายใต้สัญญาอนุญาต [MIT](LICENSE) เหมือนกับส่วนที่เหลือของโปรเจกต์
